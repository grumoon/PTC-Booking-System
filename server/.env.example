# MySQL 数据库连接（部署时唯一需要配置的内容）
# 复制此文件为 .env 并填入实际值：cp .env.example .env
DB_HOST=your_mysql_host
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=ptc_booking

# 管理员初始密码（仅 init-db.js 首次初始化时使用）
# 初始化后可在管理后台修改，此处不再使用
ADMIN_PASSWORD=your_admin_password
